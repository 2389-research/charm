version: "2"

run:
  tests: false
  timeout: 5m

linters:
  disable:
    - staticcheck
  enable:
    - bodyclose
    - goconst
    - govet
    - ineffassign
    - misspell
    - unconvert
    - unused
    - whitespace
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint

formatters:
  enable:
    - goimports

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
